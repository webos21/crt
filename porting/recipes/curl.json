{
  "schema_version": 1,
  "name": "curl",
  "version": "8.21.0",
  "source": {
    "url": "https://curl.se/download/curl-8.21.0.tar.gz",
    "archive": "curl-8.21.0.tar.gz",
    "source_dir": "curl-8.21.0",
    "sha256": "d9b327997999045a24cda50f3983e69e51c516bd8be6ef9842fc7f99135e33bb",
    "sha1": "c4a973118684745cb03c38987d131ccbce9e7ab1",
    "md5": "6e50e38c398737269ec002749bcc5d0d"
  },
  "dependencies": ["zlib", "mbedtls"],
  "build": {
    "system": "configure",
    "configure_args": [
      "--with-mbedtls=@PORT_PREFIX@",
      "--with-zlib=@PORT_PREFIX@",
      "--without-brotli",
      "--without-zstd",
      "--without-libpsl",
      "--without-libgsasl",
      "--without-ca-bundle",
      "--without-ca-path",
      "--disable-ldap",
      "--disable-ldaps",
      "--disable-ftp",
      "--disable-file",
      "--disable-telnet",
      "--disable-tftp",
      "--disable-pop3",
      "--disable-imap",
      "--disable-smb",
      "--disable-smtp",
      "--disable-gopher",
      "--disable-mqtt",
      "--disable-rtsp",
      "--disable-dict",
      "--disable-ipfs",
      "--disable-manual",
      "--disable-docs"
    ],
    "env": {
      "CFLAGS": "-O2"
    },
    "make_args": [],
    "target_overrides": {
      "windows": {
        "configure_args": [
          "--build=@CRT_MINGW_TRIPLE@",
          "--disable-threaded-resolver"
        ],
        "env": {
          "CFLAGS": "-U_WIN32 -U_WIN32_WCE -U__WIN32__ -UWIN32 -U__MINGW32__",
          "CPPFLAGS": "-U_WIN32 -U_WIN32_WCE -U__WIN32__ -UWIN32 -U__MINGW32__"
        },
        "force_include": [
          "porting/shims/win32/libtool_wrapper_compat.h"
        ]
      },
      "macos": {
        "configure_args": [
          "--disable-ipv6",
          "--disable-threaded-resolver"
        ]
      }
    }
  },
  "tests": [
    {
      "name": "http-roundtrip-static",
      "type": "compile-run",
      "source": "porting/tests/curl_http_roundtrip.c",
      "cflags": [
        "-I@PORT_PREFIX@/include"
      ],
      "link_args": [
        "@PORT_PREFIX@/lib/libcurl.a",
        "@PORT_PREFIX@/lib/libmbedtls.a",
        "@PORT_PREFIX@/lib/libmbedx509.a",
        "@PORT_PREFIX@/lib/libmbedcrypto.a",
        "@PORT_PREFIX@/lib/libz.a"
      ],
      "expect_stdout": "curl_http_roundtrip_test: ok",
      "target_overrides": {
        "windows": {
          "cflags": [
            "-U_WIN32", "-U_WIN32_WCE", "-U__WIN32__", "-UWIN32", "-U__MINGW32__"
          ]
        }
      }
    },
    {
      "name": "http-roundtrip-shared",
      "type": "compile-run",
      "source": "porting/tests/curl_http_roundtrip.c",
      "cflags": [
        "-I@PORT_PREFIX@/include"
      ],
      "expect_stdout": "curl_http_roundtrip_test: ok",
      "target_overrides": {
        "linux": {
          "link_args": [
            "@PORT_PREFIX@/lib/libcurl.so"
          ]
        },
        "macos": {
          "link_args": [
            "@PORT_PREFIX@/lib/libcurl.dylib"
          ]
        },
        "windows": {
          "cflags": [
            "-U_WIN32", "-U_WIN32_WCE", "-U__WIN32__", "-UWIN32", "-U__MINGW32__"
          ],
          "link_args": [
            "@PORT_PREFIX@/lib/libcurl.dll.a"
          ]
        }
      }
    }
  ],
  "status": {
    "linux": "shared-pass",
    "macos": "shared-pass",
    "windows": "partial"
  },
  "notes": [
    "Last port in this queue before it closes out (bzip2 -> xz -> pcre2 -> mbedtls -> curl, see TODO.md; openssl stays held back until something actually needs it). Depends on zlib and mbedtls, both already shared-pass on all three OSes.",
    "8.21.0 is curl's current stable release as of this port. --with-mbedtls=@PORT_PREFIX@/--with-zlib=@PORT_PREFIX@ point curl's own configure at this project's shared port-install prefix, where both dependencies are already installed by the time curl's own build runs (tools/crt-port-build.py's dependencies-first ordering). A real bug was found and fixed getting these flags to work at all: tools/crt-port-build.py's @PORT_PREFIX@ substitution was previously only applied to make_args/install_args, never to configure_args -- confirmed by a real build attempt showing the literal, unsubstituted string \"@PORT_PREFIX@\" in configure's own --with-mbedtls argument and resulting CPPFLAGS. Fixed generally (port_prefix_text is now computed once up front in build_configure_port() and applied to configure_args the same way as the other two).",
    "Scoped to HTTP/HTTPS for this first pass, matching this queue's own established caution (pcre2/xz/bzip2 all started narrower than upstream's full default and expanded only once solid): --disable-ftp/-file/-telnet/-tftp/-pop3/-imap/-smb/-smtp/-gopher/-mqtt/-rtsp/-dict/-ipfs and --disable-ldap/-ldaps turn off every protocol handler this first pass doesn't exercise. --without-brotli/-zstd/-libpsl/-libgsasl disable optional third-party compression/PSL/SASL libraries this project hasn't ported. --disable-manual/-docs skip building curl's own bundled documentation (not needed for a library consumer).",
    "--without-ca-bundle/--without-ca-path: no default CA trust store is baked into the build. A real deployment consuming this port would need to supply its own CA bundle (e.g. via CURLOPT_CAINFO/CURLOPT_CAINFO_BLOB at runtime) -- this project doesn't vendor or maintain a CA bundle. porting/tests/curl_http_roundtrip.c documents its own choice about certificate verification for the same reason (see that file's own comment).",
    "curl's own default (`--enable-shared`/`--enable-static`, both yes, and the default `POSIX threaded` resolver, kept rather than disabled -- see below) already builds and passes both static and shared libcurl in one pass -- no extra recipe scoping needed to get both build shapes verified together, matching this session's own new standing porting-loop discipline item in TODO.md (verify both build shapes in the same pass, not sequentially).",
    "Windows target_overrides mirror the established pattern from every other Windows recipe here: --build=@CRT_MINGW_TRIPLE@ for the config.guess gap, and undefining the _WIN32 family so curl's own build_info/configure logic treats this target as a portable POSIX host rather than native Windows -- curl's Windows-specific code (winsock-specific socket options, native TLS backends, etc.) is entirely gated behind USE_WINSOCK/_WIN32-derived macros that this technique keeps off, routing curl onto the same portable POSIX socket path already exercised by every prior Windows port in this queue.",
    "CFLAGS alone was NOT enough to undefine _WIN32 for Windows -- a real bug, found by a real build attempt: curl's own configure uses AC_EGREP_CPP (a PURE preprocessor-only check, via $CPP $CPPFLAGS, never $CFLAGS) for its 'checking if socket is prototyped' probe. With _WIN32 only undefined in CFLAGS, that specific probe still saw _WIN32 defined (clang's own default predefine for *-w64-mingw32), tried to '#include <winsock2.h>' (a header this PAL doesn't have), and curl concluded socket() wasn't usable at all -- a hard #error later in lib/transfer.c ('We cannot compile without socket() support!'), even though every actual compile/link-based probe (which DOES see CFLAGS) correctly found socket() working. Fixed by adding the identical undefines to CPPFLAGS too, not just CFLAGS -- confirmed directly: 'checking if socket is prototyped' now answers yes.",
    "**Windows build is currently blocked by a genuinely separate, real bug in mbedtls's own Windows shared build, not a curl-specific problem**: once the _WIN32/CPPFLAGS fix above let configure complete, linking curl's own shared libcurl.dll failed with `ld.lld: error: duplicate symbol` for __crt_sys_shutdown/__crt_sys_poll/__crt_sys_unlink/__crt_sys_rename/setenv -- each one defined both in this project's own c.lib (curl's normal Windows libc import library, expected) AND in libmbedcrypto.dll.a (mbedtls's own import library, NOT expected -- these are this project's internal CRT/libc symbols, nothing to do with mbedtls's real crypto API). This means mbedtls's own .dll build (porting/recipes/mbedtls.json) statically embedded this project's libc into libmbedcrypto.dll and, without any symbol-visibility control (no -fvisibility=hidden, no .def file, no explicit dllexport annotations on mbedtls's own public API -- upstream mbedtls's headers were never written with Windows DLL export control in mind), re-exported that embedded libc's own symbols right alongside mbedtls's real API. mbedtls's own standalone shared-library test still passes fine in isolation (nothing else links against both mbedtls's DLL and this project's own c.lib at once there) -- the collision only surfaces once a THIRD library (curl) links against both mbedtls's shared import library and this project's own libc import library for the same final image. Not yet fixed this session: a real fix needs either proper symbol-visibility control added to mbedtls's own Windows .dll build (so it only exports its real public API), or having curl link against mbedtls's static libraries specifically even for curl's own shared build (a common, legitimate pattern -- a shared library statically embedding a dependency rather than depending on another shared library -- but not yet wired into this recipe). Windows: configure-blocked (configure itself now succeeds; the actual link step doesn't).",
    "**macOS's own real build attempt found one more distinct bug, in `tools/crt-port-build.py` itself, not mbedtls or curl specifically**: curl's configure runs a real \"checking runtime libs availability\" probe as part of detecting the mbedTLS backend -- it compiles AND EXECUTES a tiny test program linked against -lmbedtls/-lmbedx509/-lmbedcrypto/-lz, to confirm the shared libraries it just found at link time are also loadable at runtime. That probe failed outright ('one or more libs available at link-time are not available runtime'), because mbedtls's own hand-written library/Makefile builds its .dylib files with no explicit -install_name at all (unlike every other shared-library recipe here, which drives real GNU Libtool and gets a correct one automatically) -- ld64's default records a bare 'libmbedcrypto.dylib' with no @rpath/absolute-path prefix, which dyld can never resolve via LC_RPATH (macOS's rpath mechanism only helps references already prefixed '@rpath/...'; unlike Linux's DT_RPATH/DT_RUNPATH, it does nothing for a bare name) regardless of the -Wl,-rpath flag this project's own make_env() already adds to LDFLAGS. mbedtls's own shared-library test already passed on macOS despite this exact same install-name gap, because run_port_tests()/port_test_env() already sets DYLD_LIBRARY_PATH as a runtime-loader fallback before *running* a port's own test binary -- but configure's own internal runtime probes, run as part of the *build* step (not the test step), never inherited it, since that env var was only ever set in the narrower test-running path. Fixed generally in make_env() itself (not an mbedtls- or curl-specific workaround): DYLD_LIBRARY_PATH (macOS) / LD_LIBRARY_PATH (Linux, for symmetry and future-proofing, though Linux's own rpath mechanism didn't actually need this) now point at PORT_PREFIX/lib for every subprocess this tool spawns, build steps included, not just test runs. Verified this didn't regress anything: full Linux port-test-recipes aggregate stayed clean, and curl's own static+shared tests there still pass. Not yet re-verified on macOS itself this session (no local macOS hardware) -- the user hit the original failure and this fix addresses it directly, but needs a real rebuild to confirm.",
    "**Once the library itself linked, the test programs' own compile failed with `curl/curl.h:82:10: fatal error: 'winsock2.h' file not found`**: the recipe's own `target_overrides.windows.env.CFLAGS`/`CPPFLAGS` undefine the `_WIN32` family for curl's own library build, but the `tests` entries' own `cflags` (used by `run_port_tests()` to compile `porting/tests/curl_http_roundtrip.c` against the already-installed headers/libs, a completely separate compile invocation from the library build) never got the same undefines -- so clang's default `*-w64-mingw32` predefine of `_WIN32` reached curl's own public `curl/curl.h`, which `#include`s `<winsock2.h>` whenever `_WIN32` is defined (a header this PAL doesn't have). This had never been reached before this session, since every prior Windows attempt failed earlier (configure or link). Fixed by adding the same `-U_WIN32 -U_WIN32_WCE -U__WIN32__ -UWIN32 -U__MINGW32__` set to both `http-roundtrip-static`'s and `http-roundtrip-shared`'s own `target_overrides.windows.cflags` -- consistent with the library build's own choice (a consumer compiling with a *different* `_WIN32` state than the library itself was built with would risk more than just this one missing header, since curl's own struct layouts/macros can differ by branch too).",
    "**A real Windows build re-attempt (after the earlier duplicate-symbol mbedtls-DLL blocker) got much further: configure now passes and `libcurl.la`/`libcurlu.la` themselves link cleanly (no `ld.lld: error: duplicate symbol` at all this run -- the earlier mbedtls-DLL-export-collision blocker did not reproduce here; not yet root-caused why, still tracked as open in TODO.md/`mbedtls.json` since it hasn't been deliberately fixed), but building curl's own CLI tool (`src/curl.c`/`curlinfo.c`, linked against the freshly built, still-uninstalled `libcurl.la`) failed with `call to undeclared function 'setmode'`/`'_spawnv'` and `use of undeclared identifier '_P_WAIT'`** in GNU Libtool's own generated `.libs/lt-curl.c`/`lt-curlinfo.c` wrapper source -- the exact same bug class already root-caused and fixed for libpng's own CLI/test binaries (see `libpng.json`'s notes and `porting/shims/win32/libtool_wrapper_compat.h`), but curl.json was never wired up to use that existing shim at all. One genuinely new wrinkle, not seen with libpng: libpng's recipe leaves `__MINGW32__` defined (only undefines the `_WIN32` family), so ltmain.sh's own `#elif defined __MINGW32__` rename block fires and calls the wrapper's `getcwd`/`stat`/`chmod`/`putenv`/`setmode` through their underscore-prefixed spellings, which the shim's existing macro pairs already resolve. curl.json's own CFLAGS/CPPFLAGS additionally undefine `__MINGW32__` (needed for curl's own configure/source, see the Windows target_overrides note above) -- so that rename block never fires at all, and the wrapper's literal, unconditional `setmode(1,_O_BINARY);` call site (embedded directly by ltmain.sh's `func_emit_wrapper`, not itself behind any further `#ifdef`) stays spelled exactly `setmode`, with no declaration in scope either way (`_MSC_VER` is also undefined, so the MSVC `<io.h>`-declares-it branch never ran). Fixed by (1) wiring `force_include: [\"porting/shims/win32/libtool_wrapper_compat.h\"]` into curl.json's own Windows `target_overrides` (never added when this recipe was first written, unlike libpng's), and (2) extending the shim itself with a fourth alias, `#define setmode _setmode` (the shim's existing real `_setmode()` implementation, just under the un-prefixed spelling too) -- confirmed harmless to add unconditionally: curl's own real source has exactly one reference to bare `setmode` (`lib/curl_setup.h`'s `CURL_BINMODE` macro), itself inside a permanently-dead `#ifdef MSDOS` branch on every host this project targets. `_spawnv`/`_P_WAIT` needed no shim changes -- the existing implementations were already unconditional or (in `_P_WAIT`'s case) it's a MINGW32__-independent literal call), just never reached this recipe before because `force_include` itself was missing. Not yet re-verified with a real rebuild this session (found from the user's own real Windows build log, fixed source-level, needs a real `port-rebuild-curl` to confirm both `curl.exe`/`curlinfo.exe` link and the full `port-test-curl` static/shared suite still pass).",
    "**The DYLD_LIBRARY_PATH fix above turned out to be insufficient on its own**: the user re-ran port-rebuild-curl on real macOS hardware and hit the exact same 'checking runtime libs availability... failed' error again, even with make_env()'s DYLD_LIBRARY_PATH fix in place. Root cause: configure itself is invoked as './configure', which execs via /bin/sh (its own shebang interpreter) -- and macOS strips DYLD_-prefixed environment variables across an exec of any SIP-protected system binary like /bin/sh (well-documented dyld/SIP behavior, not specific to this project's own env-passing code), so the DYLD_LIBRARY_PATH set by the parent Python subprocess call never actually survives into configure's own child test-compile-and-run. The real, durable, environment-independent fix is in mbedtls.json instead (see that recipe's own notes): three new library/Makefile patches add -install_name @rpath/$@ to the APPLE_BUILD -dynamiclib link recipes for libmbedtls.dylib/libmbedx509.dylib/libmbedcrypto.dylib, which upstream never sets at all -- this bakes the correct load-command path directly into each dylib at build time, which curl's own consumer-side -Wl,-rpath,PORT_PREFIX/lib LDFLAGS (already set unconditionally by make_env()) then resolves correctly regardless of what environment variables survive any particular exec chain. make_env()'s DYLD_LIBRARY_PATH addition is kept as defense in depth (harmless, and still useful for any future runtime-loader case not gated behind a SIP-restricted exec), but the mbedtls -install_name patch is the fix that actually matters here. Verified on Linux (WSL Ubuntu 20.04 + clang-18, full rebuild from scratch): mbedtls's own crypto-static/crypto-shared tests and curl's own http-roundtrip-static/http-roundtrip-shared tests all still pass -- the new dylib patches are inert on Linux (that Makefile target is only reached under APPLE_BUILD=1) so this is a pure regression check, not a positive macOS confirmation. Still needs a real macOS re-run to confirm.",
    "**macOS's next real rebuild advanced past the mbedTLS runtime-probe issue and exposed a separate host-SDK leakage point**: curl's `lib/macos.c` is compiled when `__APPLE__ && !USE_ARES && USE_IPV6`, defining `CURL_MACOS_CALL_COPYPROXIES` and directly including Apple's `<SystemConfiguration/SCDynamicStoreCopySpecific.h>`. That is not a Bionic/POSIX surface and should not leak into this CRT sysroot build. For this first HTTP/HTTPS curl tranche the project resolver is intentionally IPv4-only anyway, so macOS now passes `--disable-ipv6` rather than exposing host SystemConfiguration headers or patching upstream curl. This keeps the port on the same portable libc/PAL path already used by Linux while preserving a clear future expansion point: re-enable IPv6 once the CRT resolver/socket surface grows real IPv6 behavior without relying on Darwin-specific framework headers.",
    "A later macOS run showed that curl's `POSIX threaded` resolver can still time out in curl's own worker-pool completion path even though standalone `getaddrinfo()` and a new `dns_thread_test` both resolve `example.com` successfully from a pthread worker using the same AF_INET/SOCK_STREAM/IPPROTO_TCP/AI_ADDRCONFIG/AI_NUMERICSERV shape curl uses. For this port tranche, macOS therefore passes `--disable-threaded-resolver` as an honest configure capability declaration, not an upstream source patch: the mbedTLS/static/shared HTTP/HTTPS tests exercise DNS, sockets, HTTP parsing, TLS handshake, and TLS record decrypt through the synchronous resolver path while leaving curl's async resolver worker-pool fidelity as a separate CRT/PAL follow-up.",
    "curl was the FIRST port in this whole queue to actually reach out to a real internet hostname over the network rather than a self-contained local round trip, and it surfaced two real, general, previously-invisible PAL bugs, not curl-specific ones -- both root-caused by direct process/source inspection (this port's own build hanging indefinitely, not failing loudly), not guessed:",
    "(1) getaddrinfo() had no real DNS resolution at all -- it only ever handled literal numeric IP addresses via inet_pton(), returning EAI_NONAME immediately for any real hostname. Implemented a real, deliberately minimal synchronous DNS client directly in libc/src/socket.c: parses /etc/resolv.conf for a nameserver (falling back to 8.8.8.8 if missing/unreadable -- a documented simplification on Windows specifically, which has no /etc/resolv.conf at all; a fuller fix would query the OS's own configured DNS servers via IPHLPAPI's GetNetworkParams()), builds and sends a single UDP query for an A record, and parses the response (with a couple of retries and a short timeout so an unresponsive nameserver can't hang forever). Deliberately scoped: no AAAA/IPv6, no TCP fallback for truncated responses, no search-domain suffixes, no caching -- sufficient for curl's own basic HTTP/HTTPS needs, growable later if a future port needs more. Also found and fixed along the way: <sys/types.h> didn't expose size_t/time_t (real-world POSIX systems, and Android Bionic itself, do -- curl's own CURL_SIZEOF autoconf macro only #includes <sys/types.h>) and <sys/socket.h> didn't transitively include <sys/select.h> for fd_set/FD_SET/select() (real-world <sys/socket.h> commonly does -- curl's own public curl/multi.h header needs it directly), plus AF_UNIX, the IN6_IS_ADDR_* classification macros, and a real getsockopt() (mirroring the existing setsockopt() syscall plumbing across Linux/macOS/Windows) were all genuinely missing.",
    "(2) fcntl(fd, F_SETFL, O_NONBLOCK) was a pure software no-op on Linux/macOS -- it read and discarded the requested flags and always reported success, never actually marking any fd non-blocking. This is what actually caused curl_easy_perform() to hang indefinitely (not the DNS gap above, which fails fast once real -- confirmed by adding temporary instrumentation directly into curl's own lib/easy.c and lib/multi.c during this investigation, then reverting it): curl's own internal wakeup pipe (lib/socketpair.c's portable pipe()-based socketpair fallback, used by ALL curl multi-handle operations, not just DNS) sets O_NONBLOCK via fcntl() expecting a real non-blocking fd back, then does a best-effort \"drain if pending, don't block otherwise\" read on it (Curl_wakeup_consume()) on every single curl_multi_perform() call -- with O_NONBLOCK silently never taking effect, that read blocked forever on the very first call, before curl ever reached DNS resolution or opened a real network socket (confirmed directly: only ever one thread, only ever the wakeup pipe's own fds open, 0% CPU -- a real blocked read(), not a busy loop). Fixed generally in libc/src/fd.c: F_GETFL/F_SETFL now forward to the real fcntl(2) syscall on Linux/macOS (the same way F_GETFD/F_SETFD already did), which already implements O_NONBLOCK correctly at the kernel level for every fd type -- no different from how the pre-existing __crt_fd_get_cloexec/__crt_fd_set_cloexec forward F_GETFD/F_SETFD. Windows's own fcntl() backend has no unified syscall to forward to and keeps its prior (also broken, but non-regressing) no-op behavior for now, documented as a TODO comment in the code itself -- not yet hit by a real Windows curl build this session.",
    "porting/tests/curl_http_roundtrip.c intentionally remains a real external round trip against http://example.com/ and https://example.com/, not a local loopback substitute. This keeps the curl port test honest: it exercises DNS, the portable socket path, HTTP parsing, and the mbedTLS-backed HTTPS path together. A separate base CTest, tests/dns_resolve_test.c, now covers getaddrinfo()/DNS directly and supports CRT_DNS_REQUIRED=1 when DNS unavailability should be treated as a hard failure.",
    "Verified for real on macOS arm64 in this session: `cmake --build --preset macos-host-ninja-debug --target port-test-curl` passes both `http-roundtrip-static` and `http-roundtrip-shared` against real `http://example.com/` and `https://example.com/` using the mbedTLS backend (`curl_http_roundtrip_test: ok http=200 https=200`). The shared-path fix required making `tools/crt-cc` link macOS `-shared`/`-dynamiclib` outputs against this project's CRT dylibs (`@rpath/libc.dylib`, `libm.dylib`, `libdl.dylib`, `libc++.dylib`) rather than embedding the static archives and leaving network symbols to bind to host libSystem. Confirmed with `otool -L`/`nm -m`: `libcurl.dylib` now records `@rpath/libc.dylib`, and its `getaddrinfo/socket/connect/send/recv/select/poll` references come from `libc`; the rebuilt mbedTLS dylibs also depend on CRT dylibs. Linux remains shared-pass from the earlier full run.",
    "**A real, extended Windows debugging pass (directly on the user's own Windows machine, real hardware, real network) got much further and surfaced a chain of five more distinct, real bugs, all fixed -- getting HTTP working end to end for the first time ever on Windows, with a genuinely new, distinct, not-yet-fixed HTTPS crash left as the final remaining gap.** In order:",
    "(1) After the setmode/_spawnv shim wiring above got curl.exe/curlinfo.exe linking, the CLI *test build* still failed with `error: 'setmode' undeclared` at a DIFFERENT point once force_include was retested end to end -- this turned out to be the shim's own `#include <string.h>` (needed for the shim's own strlen/memcpy/strncmp calls) leaking `strchr`'s real declaration into curl's own configure-time `AC_C_UNDECLARED_BUILTIN_OPTIONS` probe (which deliberately compiles `(void) strchr;` with NO headers, expecting it to fail, to detect whether the compiler treats undeclared identifiers as real errors) -- since force_include applies the shim to literally every compile in the whole build, not just the wrapper's, `strchr` being already-declared made that probe wrongly succeed, and configure hard-errored ('cannot make ... report undeclared builtins'). Fixed by forward-declaring exactly strlen/memcpy/strncmp by hand instead of including the whole header.",
    "(2) That same force_include blast radius silently mis-detected several REAL functions as absent, without any hard error: GNU Autoconf's classic K&R-style `char FUNCNAME ();` link-only probe (`ac_fn_c_check_func`, backing dozens of curl's own `AC_CHECK_FUNC` calls) hits a hard type-conflict compile error -- silently treated as 'no, doesn't exist' rather than a real failure -- whenever the shim had ALSO already declared that same symbol with its real, correctly-typed prototype first. `<unistd.h>` (for `pipe()`), `<stdlib.h>` (for `realpath()`), and `<spawn.h>`'s own transitive `<sched.h>` (for `sched_yield()`) all did exactly this, flipping `checking for pipe/realpath/sched_yield... yes` to `no`. `checking for pipe... no` specifically turned out to be why `curl_easy_perform()` failed outright with 'Out of memory' on its very first call: curl's own internal wakeup-pipe mechanism (`lib/socketpair.c`) genuinely needs `pipe()`, believed it didn't have it, and picked a different, broken path instead. Fixed by minimizing the shim's own header footprint to exactly what its own code directly calls (forward-declared `environ`/`malloc`/`free` by hand instead of `<unistd.h>`/`<stdlib.h>`; kept `<spawn.h>`/`<sys/wait.h>`, unavoidable for `posix_spawn()`'s own opaque types, accepting `sched_yield`'s own residual, confirmed-non-fatal misdetection as a documented, bounded cost -- see `libtool_wrapper_compat.h`'s own notes for the full trail).",
    "(3) Once `pipe()` was correctly detected, the test PROGRAM'S OWN compile (not curl's library build) failed with `curl/curl.h:82:10: fatal error: 'winsock2.h' file not found` -- the recipe's `target_overrides.windows.env.CFLAGS`/`CPPFLAGS` undefine the `_WIN32` family for curl's own library build, but the `tests` entries' own `cflags` never got the same treatment (a completely separate compile invocation), so clang's default `_WIN32` predefine reached curl's public header, which `#include`s `<winsock2.h>` whenever `_WIN32` is defined. Fixed by adding the identical `-U_WIN32` family to both `http-roundtrip-static`'s and `http-roundtrip-shared`'s own `target_overrides.windows.cflags`.",
    "(4) With the test finally compiling and running, `curl_easy_perform()` hung indefinitely again -- confirmed via direct process inspection (Get-Process showing 0% CPU minutes past the test's own 20-second CURLOPT_TIMEOUT, which never fired) to be the *exact same* fcntl(F_SETFL, O_NONBLOCK)-is-a-no-op bug already fixed for Linux/macOS earlier in this same porting pass, just never reachable on Windows until curl's own configure could correctly detect `pipe()` (bug 2 above) and actually exercise this code path for the first time. Fixed for real this time: `libc/src/arch/windows/common/syscall.c` gained real per-fd O_NONBLOCK tracking (`fd_nonblock[]`) and `__crt_fd_get_status_flags()`/`__crt_fd_set_status_flags()` -- SOCKET fds via winsock's own `ioctlsocket(FIONBIO)` (already loaded via GetProcAddress), pipe fds (this project's fd table classifies pipes as plain `CRT_FD_KIND_FILE`, so `GetFileType()==FILE_TYPE_PIPE` is the only way to tell them apart, the same technique `__crt_sys_lseek()`'s own ESPIPE check already uses) via `SetNamedPipeHandleState(PIPE_NOWAIT)` -- a real Win32 mechanism that works on `CreatePipe()`'s own handles despite never going through `CreateNamedPipe()` directly, since they're secretly backed by named-pipe kernel objects under the hood. `__crt_sys_read()`/`__crt_sys_write()` translate the resulting Win32-specific non-blocking signals (`ERROR_NO_DATA` on an empty PIPE_NOWAIT read; a real Win32 quirk where a full PIPE_NOWAIT write 'succeeds' with 0 bytes written instead of erroring) into `EAGAIN`, gated on `fd_nonblock[]` so a genuinely broken/closed pipe elsewhere keeps its ordinary `EPIPE`/`ENODATA` mapping. `libc/src/fd.c`'s F_GETFL/F_SETFL cases now call these uniformly on every OS, same as F_GETFD/F_SETFD already did via `__crt_fd_get_cloexec`/`__crt_fd_set_cloexec`. Verified: full local libc rebuild + `ctest` 85/85, no regressions.",
    "(5) With the hang gone, `curl_easy_perform()` returned a clean CURLE_SEND_ERROR ('Failed sending data to the peer') instead -- real, visible progress (DNS resolved, TCP connected) but still failing. `curl -v`-equivalent tracing (CURLOPT_VERBOSE) showed a real TCP connection established (`* Established connection to example.com ... from 0.0.0.0 port NNNNN`) immediately followed by `* Send failure: Transport endpoint is not connected` (ENOTCONN) on the very first `send()`. Root-caused with a minimal standalone probe (raw `socket()`/`fcntl(O_NONBLOCK)`/`connect()`/`select()`/`getsockopt(SO_ERROR)`/`send()` sequence, no curl involved) run through the same real toolchain: `connect()` correctly returned `EINPROGRESS` (a related, smaller bug fixed alongside this one -- `__crt_sys_connect()` was mapping non-blocking connect's real Winsock `WSAEWOULDBLOCK` through the same generic `map_wsa_error()` every other socket call uses, landing on `EAGAIN`, the wrong POSIX errno for connect() specifically; real POSIX connect(2) signals 'in progress' with `EINPROGRESS`, a distinct errno curl's own connection code specifically checks for -- fixed by special-casing `WSAEWOULDBLOCK` to `EINPROGRESS` only in `__crt_sys_connect()`), `select()` correctly reported the socket writable, and `getsockopt(SOL_SOCKET, SO_ERROR)` correctly reported 0 (no error) -- yet `send()` still failed `WSAENOTCONN` on the very first attempt, and a bare retry after a ~200ms delay (with no further connect()/select() calls in between) succeeded outright. This is a real, reproducible Winsock quirk on this environment, not a caller bug or a misconfigured socket: Winsock's own AFD (Ancillary Function Driver) socket layer appears to update its internal 'connected' bookkeeping on a very slightly different schedule than the TCP/IP driver posts the FD_CONNECT completion select()/getsockopt() already both correctly observed as done. Fixed generally (not curl-specific): a new `map_wsa_send_recv_error()` reinterprets `WSAENOTCONN` as `EAGAIN`, but only for a socket already known to be in non-blocking mode (`fd_nonblock[]`) -- exactly the scenario where a correct non-blocking I/O caller (curl, or anything else) already has its own ordinary EAGAIN-retry loop, which then naturally retries and succeeds once the race resolves, matching what the standalone probe already confirmed. A genuinely blocking socket hitting a real 'never connected' WSAENOTCONN is unaffected. Applied to `__crt_sys_read()`/`__crt_sys_write()`'s socket branches and to `__crt_sys_sendto()`/`__crt_sys_recvfrom()` (the latter two also switched to calling the real, canonical `winsock.send()`/`winsock.recv()` directly instead of `sendto()`/`recvfrom()` with a NULL target, a smaller, independently-real hygiene fix that was NOT by itself sufficient to resolve this bug -- confirmed directly, the failure reproduced identically either way -- but is kept as the more correct call regardless).",
    "**Result of all five fixes: the HTTP round trip now passes completely, for the first time ever on Windows** -- a real `curl_easy_perform()` against `http://example.com/` returns a genuine `HTTP/1.1 200 OK` with real response headers from Cloudflare, verified directly on the user's own Windows machine. **The HTTPS round trip does not** -- it fails with a hard, deterministic crash (`STATUS_ACCESS_VIOLATION` / SIGSEGV, exit code `3221225477`), reproduced twice in a row, occurring right after `* mbedTLS: Connecting to example.com:443` is printed (i.e., somewhere inside curl's own mbedTLS vtls backend beginning the TLS handshake, or inside mbedTLS itself). NOT YET ROOT-CAUSED -- this is a new, distinct bug from all five above, first reached only once the HTTP path itself started working end to end, and needs its own dedicated investigation (most likely candidate: something in curl's mbedTLS `Curl_mbedtls_send`/`Curl_mbedtls_recv` callback plumbing feeding raw socket I/O through `mbedtls_ssl_set_bio()`, given every socket-I/O-adjacent function just changed above, but not confirmed). Windows status: `partial` (a real, useful, verified subset -- HTTP -- works end to end; HTTPS is a known, open, unresolved crash). See `HISTORY.md`'s dated entry for the full trail; `porting/shims/win32/libtool_wrapper_compat.h`'s own notes cover fixes (1)-(2) in more depth."
  ]
}
