# Android libunwind Source Policy

See `../libcxx/README.md`. This metadata directory represents Android's paired
`platform/external/libunwind` checkout; the fetched source stays under the
active preset's `out/<preset>/external/llvm-runtimes/libunwind/` directory.
