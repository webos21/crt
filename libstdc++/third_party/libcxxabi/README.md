# Android libc++abi Source Policy

See `../libcxx/README.md`. This metadata directory represents Android's paired
`platform/external/libcxxabi` checkout; the fetched source stays under the
active preset's `out/<preset>/external/llvm-runtimes/libcxxabi/` directory.
